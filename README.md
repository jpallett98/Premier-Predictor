# FanRank V15.2

Conservative V15 rebuild based on the working V14.1 interface.
The existing navigation/UI is preserved. V15 prediction database functions are isolated so database errors cannot stop the interface.

If the V15 prediction tables have not already been created, run fanrank-v15-predictions-schema.sql in Supabase SQL Editor once.
